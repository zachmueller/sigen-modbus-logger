{
 "_": "Copy to config.json and edit. Every key below is optional except host: drop the ones you do not want to override and config.py's default applies. Keys starting with _ are ignored, so this line can stay. null means 'use the default'.",

 "host": "192.168.1.50",
 "port": 502,
 "timeout_s": 6.0,

 "plant_unit": 247,
 "inverter_unit": 1,

 "install_dir": "~/sigen",
 "data_dir": null,
 "log_dir": null,

 "python": "/usr/bin/python3",
 "launchd_label": "local.sigen-logger",
 "run_as_user": "youruser",

 "__viewer": "serve.py, the local web viewer. It only reads the archive on disk -- it never opens a Modbus client. web_bind 0.0.0.0 means anyone on your LAN can read your telemetry; use 127.0.0.1 and an SSH tunnel if you would rather they could not.",
 "web_port": 8787,
 "web_bind": "0.0.0.0",
 "web_launchd_label": "local.sigen-viewer",
 "web_default_hours": 6,
 "web_show_identity": false,

 "fast_period_s": 2,
 "rotate_minutes": 60,
 "keep_days": 0,

 "degrade_after": 5,
 "degrade_probe_s": 30,
 "recycle_s": 3600,
 "max_lag_s": 5,
 "gap_log_quiet_s": 60,
 "bucket_s": 300,

 "manifest_identity": true,

 "__offsite": "sync.py, the offsite copy. The one part that needs a dependency (boto3) and a network credential, and it is OFF by default -- an archive is nobody's business until its owner says so. Credentials come from a named ~/.aws profile, NEVER from this file. The uploader sends rotated .bin.gz and manifests only, never the open .bin, so the current hour reaches S3 on the next rotation.",
 "s3_bucket": null,
 "s3_region": "us-east-1",
 "s3_prefix": "raw/",
 "aws_profile": null,
 "sync_enabled": false,
 "sync_launchd_label": "local.sigen-sync"
}
